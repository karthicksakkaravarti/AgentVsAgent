{
  "name": "benchmark",
  "version": "1.0.0",
  "description": "Benchmark runner, scorer, and report generator for Agent vs Agent",
  "main": "dist/runner.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/runner.js",
    "dev": "ts-node src/runner.ts"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.0"
  }
}
